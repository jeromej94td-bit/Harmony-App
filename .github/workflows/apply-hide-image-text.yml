name: Apply Dev Studio image text fix

on:
  push:
    branches:
      - fix/devstudio-hide-image-text

permissions:
  contents: write

jobs:
  apply-and-test:
    if: ${{ !contains(github.event.head_commit.message, 'fix: keep image when hiding Dev Studio text') }}
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          ref: fix/devstudio-hide-image-text
          fetch-depth: 0

      - name: Apply source edit
        run: python .github/scripts/apply_hide_image_text.py

      - name: Set up JDK 21
        uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: '21'

      - name: Set up Gradle 9.3.1
        uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: '9.3.1'

      - name: Run unit tests
        run: gradle :app:test --stacktrace

      - name: Commit production fix
        run: |
          rm -f .github/scripts/apply_hide_image_text.py
          rm -f .github/workflows/apply-hide-image-text.yml
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git commit -m "fix: keep image when hiding Dev Studio text"
          git push origin HEAD:fix/devstudio-hide-image-text
