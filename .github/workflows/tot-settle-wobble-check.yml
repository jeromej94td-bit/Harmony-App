name: Tot Settle Wobble Check

on:
  push:
    branches:
      - fix/tot-settle-wobble

jobs:
  verify:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Verify settle wobble contract
        run: python3 scripts/verify_tot_settle_wobble.py
