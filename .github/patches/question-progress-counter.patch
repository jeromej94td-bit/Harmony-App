diff --git a/app/src/main/java/com/example/ui/screens/QuizRunnerScreen.kt b/app/src/main/java/com/example/ui/screens/QuizRunnerScreen.kt
--- a/app/src/main/java/com/example/ui/screens/QuizRunnerScreen.kt
+++ b/app/src/main/java/com/example/ui/screens/QuizRunnerScreen.kt
@@ -655,14 +655,32 @@
                                 fontWeight = FontWeight.Bold,
                                 color = Color.White
                             )
+                            Spacer(modifier = Modifier.height(2.dp))
+                            Text(
+                                text = "${activeRun.currentIndex + 1}/$totalLen",
+                                fontSize = 13.sp,
+                                fontWeight = FontWeight.Bold,
+                                color = Color.White.copy(alpha = 0.92f),
+                                modifier = Modifier.testTag("runner_question_counter")
+                            )
                             Spacer(modifier = Modifier.height(4.dp))
                             Box(
                                 modifier = Modifier
                                     .width(130.dp)
                                     .height(4.dp)
                                     .clip(RoundedCornerShape(2.dp))
-                                    .background(Color.White.copy(alpha = 0.35f))
-                            )
+                                    .background(Color.White.copy(alpha = 0.24f))
+                            ) {
+                                val fraction = ((activeRun.currentIndex + 1).toFloat() / totalLen.coerceAtLeast(1))
+                                    .coerceIn(0f, 1f)
+                                Box(
+                                    modifier = Modifier
+                                        .fillMaxWidth(fraction)
+                                        .fillMaxHeight()
+                                        .clip(RoundedCornerShape(2.dp))
+                                        .background(Color.White.copy(alpha = 0.88f))
+                                )
+                            }
                         }
 
                         Box(modifier = Modifier.size(36.dp))
@@ -714,9 +732,10 @@
 
                         Text(
                             text = if (activeRun.isFinished) "$totalLen/$totalLen" else "${activeRun.currentIndex + 1}/$totalLen",
-                            fontSize = 12.sp,
+                            fontSize = 13.sp,
                             fontWeight = FontWeight.Bold,
-                            color = Color.White.copy(alpha = 0.75f)
+                            color = Color.White.copy(alpha = 0.92f),
+                            modifier = Modifier.testTag("runner_question_counter")
                         )
                     }
                 }
diff --git a/app/src/main/java/com/example/ui/screens/PandaEitherOrScreen.kt b/app/src/main/java/com/example/ui/screens/PandaEitherOrScreen.kt
--- a/app/src/main/java/com/example/ui/screens/PandaEitherOrScreen.kt
+++ b/app/src/main/java/com/example/ui/screens/PandaEitherOrScreen.kt
@@ -103,5 +103,11 @@
     val displayPair = questionIndex?.let(displayPack.pairs::get)
     val isMatch = userChoice != null && userChoice == partnerChoice
     val remaining = questionOrder.size - orderPosition
+    val overallCurrent = if (questionIndex == null) {
+        rawPack.pairs.size
+    } else {
+        (completedBeforeStart.size + orderPosition + 1)
+            .coerceAtMost(rawPack.pairs.size)
+    }
 
     fun localizedTemplate(source: String, vararg values: Pair<String, String>): String {
@@ -130,6 +136,12 @@
                 }
                 Column(modifier = Modifier.weight(1f), horizontalAlignment = Alignment.CenterHorizontally) {
                     Text("🐼 ${LanguageManager.tr("Entweder oder", appLanguage)}", color = HarmonyText, fontSize = 21.sp, fontWeight = FontWeight.ExtraBold)
+                    Text(
+                        text = "$overallCurrent/${rawPack.pairs.size}",
+                        color = HarmonyPink,
+                        fontSize = 13.sp,
+                        fontWeight = FontWeight.Bold
+                    )
                     Text(
                         if (questionIndex == null) LanguageManager.tr("Alle Fragen beantwortet", appLanguage)
                         else localizedTemplate("{count} offen · keine Wiederholungen", "count" to remaining.toString()),
